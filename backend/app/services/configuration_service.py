class ConfigurationService:
    @staticmethod
    def load_user_configuration(user_key):
        # Fetch user configuration from the database
        pass

    @staticmethod
    def save_user_configuration(user_key, config):
        # Save the configuration in the database
        pass

    @staticmethod
    def update_user_configuration(user_key, updates):
        # Apply updates to the config in the database and save
        pass